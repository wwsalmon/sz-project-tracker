type Project @model @auth(rules: [{allow: owner}]) {
  id: ID!
  name: String!
  events: [Event]!
}

type Event @model @auth(rules: [{allow: owner}]) {
  id: ID!
  projectID: String!
  time: AWSDate!
  note: String!
}